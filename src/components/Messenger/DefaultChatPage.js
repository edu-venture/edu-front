import React from "react";

const DefaultChatPage = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        background: "#fff",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      대화 상대를 선택하세요.
    </div>
  );
};

export default DefaultChatPage;
